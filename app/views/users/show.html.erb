<div class="user">
  <div class="user_info">
    <% if user_signed_in? %>
      <div class="userName">
      こんにちは<%= current_user.name %>さん<br>
      </div>
      <u><p><%= link_to "ログアウトはコチラ", destroy_user_session_path, {method: :delete, class: "other_login"} %></p></u><br>
      <u><p><%= link_to "アカウント削除はコチラ", user_registration_path, {method: :delete, class: "other_login", data: {confirm: "アカウント『#{current_user.name}』を削除しますか？"}} %></p></u>
    <% else %>
      <div class="userName">
      ようこそゲストさん<br>
      </div>
      <p>EasyTagの機能を使うためにまずはユーザー登録をしましょう！</p><br>
      <div class="signUp">
      <a href="/users/sign_up", class="other_login"><u><p>新規登録はコチラ</p></u></a><br>
      <a href="/users/sign_in", class="other_login"><u><p>ログインはコチラ</p></u></a>
      </div>
    <% end %>
  </div>
</div>